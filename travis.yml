language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev" # 3.5 development branch
  - "nightly" # currently points to 3.6-dev
# Install dependencies
install: "pip install --requirement tests/requirements.txt"
# Run tests
script: nosetests